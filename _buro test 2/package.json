{
  "name": "luxe-corsa-buro-test-2",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "The private paddock — a redesign of the Luxe Corsa proposition. Eight acts, two pins, one signature: PLAN → SELECT → ENTER.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "plan": "python3 tools/extract-plan.py",
    "frames": "python3 tools/build-frames.py",
    "assets": "npm run plan && npm run frames",
    "publish": "node tools/publish.mjs",
    "hub": "node tools/build-hub.mjs"
  },
  "dependencies": {
    "gsap": "^3.15.0",
    "lenis": "^1.3.26",
    "three": "^0.180.0"
  },
  "devDependencies": {
    "vite": "^8.2.2"
  }
}
